import React from 'react';
import { View, Text } from 'react-native';

const TripHistory = () => {
  const test = '';
  return (
    <View>
      <Text>TripHistory {test}</Text>
    </View>
  );
};

export default TripHistory;
